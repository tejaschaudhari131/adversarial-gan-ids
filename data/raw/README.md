# Raw datasets (not committed)

Place official CSVs here. See [docs/DATASETS.md](../../docs/DATASETS.md) for
download URLs and the exact folder names:

- `cicids2017/`
- `cse-cic-ids2018/`
- `unsw-nb15/`
- `cic-iot-2023/` (optional)

Without these folders, use `--synthetic` to generate a same-schema stand-in.
